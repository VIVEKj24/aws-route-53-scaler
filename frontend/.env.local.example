# The browser always calls its own /api/* path.
# Next.js rewrites /api/* → <API_URL>/api/* server-side.
# Change API_URL if your FastAPI backend runs on a different host/port.
NEXT_PUBLIC_API_URL=http://localhost:3000
API_URL=http://127.0.0.1:8000
